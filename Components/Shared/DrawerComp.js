const Handler = ({ testDrawer }) => {
  return (
    <div
      className={`absolute top-0 left-0 h-[100vh] w-[100vw] text-white animate__animated animate__fadeIn`}
      style={{ background: "rgba(0, 0, 0, .5)" }}
    ></div>
  );
};

export default Handler;
